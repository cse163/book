{"metadata":{"kernelspec":{"name":"python3","display_name":"Python 3","language":"python"}},"nbformat":4,"nbformat_minor":2,"cells":[{"cell_type":"markdown","source":["# Jupyter Notebooks\n","```{jupyter-info}\n","```"],"metadata":{}},{"cell_type":"markdown","source":["This is a Jupyter Notebook! It's a lot like Python's interactive mode with two big differences:\n","* You can put formatted text in a notebook (like this text right here!)\n","* You break your interaction into various cells that you can go back and edit or run in any order!\n","\n","Below, is a notebook **cell** that contains some Python code. You can run it by clicking on it and hitting `Shift+Enter`."],"metadata":{},"attachments":{}},{"cell_type":"code","execution_count":1,"source":["print('Hello world')"],"outputs":[{"output_type":"stream","name":"stdout","text":["Hello world\n"]}],"metadata":{}},{"cell_type":"code","execution_count":2,"source":["1 + 1"],"outputs":[{"output_type":"execute_result","data":{"text/plain":["2"]},"metadata":{},"execution_count":2}],"metadata":{}},{"cell_type":"markdown","source":["Notice in the second cell, I didn't have to print the result! This is because a Jupyter Notebook always displays the value of the last line of code in it for you! Here is a clearer example of that concept."],"metadata":{},"attachments":{}},{"cell_type":"code","execution_count":3,"source":["x = min(1, 2)\n","y = min(4, 5)\n","x"],"outputs":[{"output_type":"execute_result","data":{"text/plain":["1"]},"metadata":{},"execution_count":3}],"metadata":{}},{"cell_type":"markdown","source":["You might be wondering, is this a lot like Ed and it's snippets? While they might look similar (code with text in between), they actually operate quite differently! \n","\n","* A **snippet** in Ed is its own independent program. When you run it, you get a blank slate.\n","\n","* A **cell** in a Jupyter Notebook are connected because they all share the same enviornment to store variables! This is sometimes called the notebook's \"hidden state\" because behind the scenes it is storing the results of all the previous cells! For example, the next cell uses the `x` variable that was defined earlier in this notebook (this would not work with an Ed snippet)."],"metadata":{},"attachments":{}},{"cell_type":"code","execution_count":4,"source":["x"],"outputs":[{"output_type":"execute_result","data":{"text/plain":["1"]},"metadata":{},"execution_count":4}],"metadata":{}},{"cell_type":"markdown","source":["**⚠️Warning:** This behavior can be useful, but commonly does more harm than good! For example, we mentioned we can run cells in any order. That means if multiple cells read/update the same variable, now the behavior of your notebook depends on the order in which you ran the cells! A common trick is to always restart the notebook and run from the top, which you can do with the menu option up top `Restart & Run All`. "],"metadata":{},"attachments":{}},{"cell_type":"markdown","source":["In CSE 163, we will commonly share notebooks for things like lecture notes and demos because it makes it easier to tell a narrative between code blocks (for example, when we don't want to load in a dataset every single time). There is a lot more complexity to these Jupyter notebooks, but right now all we need is the notion of running these cells! 😊 "],"metadata":{},"attachments":{}}]}